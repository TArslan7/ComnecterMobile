/// Authentication configuration
/// Firebase Authentication is enabled by default
class AuthConfig {
  /// Firebase Authentication is enabled
  static const bool useFirebaseAuth = true;
  
  /// Whether to show debug information about which auth service is being used
  static const bool showDebugInfo = false;
}
